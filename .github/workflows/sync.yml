name: Upstream Sync

permissions:
  contents: write

on:
  schedule:
    - cron: "0 19 * * *" # 北京时间凌晨3点 (UTC 19:00)
  workflow_dispatch:

jobs:
  sync_latest_from_upstream:
    name: Sync latest commits from upstream repo
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.fork }}

    steps:
      # Step 1: run a standard checkout action
      - name: Checkout target repo
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # Step 2: run the sync action
      - name: Sync upstream changes
        id: sync
        uses: aormsby/Fork-Sync-With-Upstream-action@v3.4
        with:
          upstream_sync_repo: cmliu/edgetunnel
          upstream_sync_branch: main
          target_sync_branch: main
          target_repo_token: ${{ secrets.GITHUB_TOKEN }} # automatically generated, no need to set

          # Set test_mode true to run tests instead of the true action!!
          test_mode: false

      # Step 3: 自动修改 wrangler.toml 中的 name 字段
      - name: Customize wrangler.toml
        if: steps.sync.outputs.has_new_commits == 'true'
        run: |
          # 检查 wrangler.toml 是否存在
          if [ -f "wrangler.toml" ]; then
            # 使用 sed 替换 name 字段，支持任意原始值
            sed -i 's/^name = ".*"/name = "mysok"/' wrangler.toml
            echo "✅ wrangler.toml 中的 name 已修改为 mysok"
          else
            echo "⚠️ wrangler.toml 文件不存在，跳过修改"
          fi

      # Step 4: 提交修改
      - name: Commit customization
        if: steps.sync.outputs.has_new_commits == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # 检查是否有修改
          if git diff --quiet wrangler.toml; then
            echo "📝 wrangler.toml 没有变化，无需提交"
          else
            git add wrangler.toml
            git commit -m "chore: 自动修改 wrangler.toml name 为 mysok"
            git push
            echo "✅ 已提交并推送 wrangler.toml 的修改"
          fi

      - name: Sync check
        if: failure()
        run: |
          echo "[Error] 由于上游仓库的 workflow 文件变更，导致 GitHub 自动暂停了本次自动更新，你需要手动 Sync Fork 一次，详细教程请查看项目README.md "
          echo "[Error] Due to a change in the workflow file of the upstream repository, GitHub has automatically suspended the scheduled automatic update. You need to manually sync your fork. Please refer to the project README.md for instructions. "
          exit 1
